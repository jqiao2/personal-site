---
// The former import page moved to /activities/settings. Kept as a redirect so
// old links and an in-flight Strava OAuth return don't 404.
export const prerender = false;
return Astro.redirect('/activities/settings', 301);
---
